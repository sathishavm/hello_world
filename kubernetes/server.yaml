apiVersion: v1
kind: Service
metadata:
  name: hello-world
  namespace: zendesk-canary
  labels:
    project: hello-world
    role: app-server
    team: eng-productivity
    proxy: enabled
spec:
  ports:
  - port: 80
    targetPort: main
  selector:
    project: hello-world
    role: app-server
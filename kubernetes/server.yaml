apiVersion: v1
kind: Service
metadata:
  name: hello-world
  labels:
    project: hello-world
    role: app-server
    team: bre
    proxy: enabled
spec:
  type: NodePort
  ports:
  - port: 80
    targetPort: main
  selector:
    project: hello-world
    role: app-server